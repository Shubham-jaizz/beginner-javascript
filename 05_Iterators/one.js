//for
for (let index = 0; index < 5; index++) {
   console.log(index);
    
}

///////////////////////////////////////////same as c++ for loops, break continue////////////////////////////////


for await (const element of object) {
   this.closeUp();
   // it encoles the setup time
}

